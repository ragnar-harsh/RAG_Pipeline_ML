# RAG_Pipeline_ML




pip install "unstructured[all-docs]"
%pip install -Uq langchain_chroma 
%pip install -Uq langchain langchain_community
pip install langchain_ollama langchain_huggingface
%pip install -Uq python_dotenv
pip install sentence-transformers





Questions - 
1. What are the two main components of Transformer Architecture?
2. How many layers does the base Transformer model use in both encoder and decoder?
3. What is the dimentionality (dmodel) used in the base transformer model?


